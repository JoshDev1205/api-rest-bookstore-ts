# Migration `20191013092802-initial`

This migration has been generated by Joseph Ospina Tafur at 10/13/2019, 9:28:02 AM.
You can check out the [state of the datamodel](./datamodel.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "lift"."User" (
  "email" TEXT NOT NULL DEFAULT ''  ,
  "id" TEXT NOT NULL   ,
  "password" TEXT NOT NULL DEFAULT ''  ,
  PRIMARY KEY ("id")
);

CREATE TABLE "lift"."Author" (
  "country" TEXT NOT NULL DEFAULT ''  ,
  "id" TEXT NOT NULL   ,
  "name" TEXT NOT NULL DEFAULT ''  ,
  PRIMARY KEY ("id")
);

CREATE TABLE "lift"."Book" (
  "author" TEXT    REFERENCES "Author"(id) ON DELETE SET NULL,
  "description" TEXT NOT NULL DEFAULT ''  ,
  "id" TEXT NOT NULL   ,
  "price" INTEGER NOT NULL DEFAULT 0  ,
  "quantity" INTEGER    ,
  "title" TEXT NOT NULL DEFAULT ''  ,
  PRIMARY KEY ("id")
);

CREATE UNIQUE INDEX "lift"."User.id" ON "User"("id")

CREATE UNIQUE INDEX "lift"."User.email" ON "User"("email")

CREATE UNIQUE INDEX "lift"."Author.id" ON "Author"("id")

CREATE UNIQUE INDEX "lift"."Book.id" ON "Book"("id")
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration ..20191013092802-initial
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,30 @@
+datasource db {
+  provider = "sqlite"
+  url      = "file:dev.db"
+}
+
+generator photon {
+  provider = "photonjs"
+}
+
+model User {
+  id       String @default(cuid()) @id @unique
+  email    String @unique
+  password String
+}
+
+model Author {
+  id      String @default(cuid()) @id @unique
+  name    String
+  country String
+  books   Book[]
+}
+
+model Book {
+  id          String @default(cuid()) @id @unique
+  title       String
+  description String
+  quantity    Int?   @default(0)
+  author      Author
+  price       Int
+}
```

## Photon Usage

You can use a specific Photon built for this migration (20191013092802-initial)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20191013092802-initial'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
